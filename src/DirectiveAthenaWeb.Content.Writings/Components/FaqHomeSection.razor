@* ------------------------------------------------------------------------------------------------------------------ *@
@* Imports
@* ------------------------------------------------------------------------------------------------------------------ *@
@using DirectiveAthenaWeb.Content.Faq
@using DirectiveAthenaWeb.Services.Localization.Resources
@using Microsoft.Extensions.Localization

@namespace DirectiveAthenaWeb.Components
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Descriptors
@* ------------------------------------------------------------------------------------------------------------------ *@
@inject IFaqContentRepository WorldFaqRepository
@inject IFaqContentManager WorldFaqManager
@inject IStringLocalizer<Shared> LocalizerShared
@inject IStringLocalizer<Tags> LocalizerTags
@inject NavigationManager NavigationManager
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Html
@* ------------------------------------------------------------------------------------------------------------------ *@
<section id="world-faq" class="py-32">
    <MudContainer MaxWidth="MaxWidth.Large">
        <div class="d-flex justify-space-between align-end mb-12">
            <div>
                <MudText Typo="Typo.h2" Class="mb-4">@LocalizerShared[Shared.HomeWorldFaqTitle]</MudText>
                <MudText Typo="Typo.body1"
                         Class="text-secondary opacity-70">@LocalizerShared[Shared.HomeWorldFaqSubtitle]</MudText>
            </div>
            <MudButton Variant="Variant.Text" Color="Color.Secondary" Href="/world-faq"
                       EndIcon="@Icons.Material.Filled.ArrowForward"
                       Class="d-none d-sm-flex">@LocalizerShared[Shared.HomeWorldFaqViewAll]</MudButton>
        </div>

        @if (!_isLoaded) {
            <MudGrid Spacing="6">
                @for (int i = 0; i < 2; i++) {
                    <MudItem xs="12" md="6">
                        <MudCard Class="card-glass h-100 d-flex flex-column">
                            <MudCardContent Class="flex-grow-1 pa-8">
                                <MudProgressLinear Color="Color.Secondary" Indeterminate="true" Class="mb-4"/>
                                <MudProgressLinear Color="Color.Secondary" Indeterminate="true" Class="mb-4"
                                                   Style="height: 10px;"/>
                                <MudProgressLinear Color="Color.Secondary" Indeterminate="true" Class="mb-4"
                                                   Style="height: 10px; width: 80%;"/>
                                <MudProgressLinear Color="Color.Secondary" Indeterminate="true"
                                                   Style="height: 10px; width: 50%;"/>
                            </MudCardContent>
                        </MudCard>
                    </MudItem>
                }
            </MudGrid>
        }
        else if (WorldFaqs is not null) {
            if (!WorldFaqs.Any()) {
                <MudCard Class="card-glass pa-8 text-center border-dashed">
                    <MudText Typo="Typo.h6" Class="opacity-50">@LocalizerShared[Shared.HomeWorldFaqEmpty]</MudText>
                </MudCard>
            }
            else {
                <MudGrid Spacing="6">
                    @foreach (FaqContent rule in WorldFaqs) {
                        <MudItem xs="12" md="6">
                            <MudCard Class="card-glass h-100 d-flex flex-column" @onclick="@(() => ViewRules())"
                                     Style="cursor: pointer;">
                                <MudCardContent Class="flex-grow-1 pa-8">
                                    <MudText Typo="Typo.h4"
                                             Class="mb-4 SpaceGrotesk">@WorldFaqManager.GetLocalizedQuestion(rule)</MudText>
                                    <MudText Typo="Typo.body2" Class="text-secondary mb-6"
                                             Style="line-height: 1.6;">
                                        @WorldFaqManager.GetLocalizedAnswer(rule)
                                    </MudText>
                                    @if (rule.Tags.Count > 0) {
                                        <div class="d-flex gap-1">
                                            @foreach (string tag in rule.Tags.Take(2)) {
                                                <span class="px-2 py-0 rounded"
                                                      style="background: rgba(0, 229, 255, 0.1); color: var(--neon-cyan); font-size: 0.7rem; border: 1px solid rgba(0, 229, 255, 0.2);">@LocalizerTags[tag]</span>
                                            }
                                        </div>
                                    }
                                </MudCardContent>
                            </MudCard>
                        </MudItem>
                    }
                </MudGrid>
            }
        }

        <div class="d-flex justify-center mt-12 d-sm-none">
            <MudButton Variant="Variant.Outlined" Color="Color.Secondary"
                       Href="/world-faq">@LocalizerShared[Shared.HomeWorldFaqViewAll]</MudButton>
        </div>
    </MudContainer>
</section>

@* ------------------------------------------------------------------------------------------------------------------ *@
@* Code
@* ------------------------------------------------------------------------------------------------------------------ *@

@code {
    private List<FaqContent>? WorldFaqs { get; set; }
    private bool _isLoaded;

    // -----------------------------------------------------------------------------------------------------------------
    // Methods
    // -----------------------------------------------------------------------------------------------------------------
    protected override async Task OnInitializedAsync() {
        IEnumerable<FaqContent> rules = await WorldFaqRepository.GetAllAsync();
        WorldFaqs = rules
            .Where(rule => rule.ShowOnHomepage)
            .OrderByDescending(rule => rule.LastModifiedAt)
            .Take(4)
            .ToList();

        _isLoaded = true;
    }

    private void ViewRules() {
        NavigationManager.NavigateTo("/world-faq");
    }

}
