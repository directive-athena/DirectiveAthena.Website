/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./src/DirectiveAthena.Website/wwwroot/ts/interop/clipboard.ts"
/*!*********************************************************************!*\
  !*** ./src/DirectiveAthena.Website/wwwroot/ts/interop/clipboard.ts ***!
  \*********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   registerClipboard: () => (/* binding */ registerClipboard)
/* harmony export */ });
const registerClipboard = () => {
    window.copyToClipboard = async (text) => {
        try {
            if (navigator.clipboard && window.isSecureContext) {
                await navigator.clipboard.writeText(text);
                return true;
            }
        }
        catch (error) {
            console.warn("Clipboard API copy failed, falling back.", error);
        }
        try {
            const textarea = document.createElement("textarea");
            textarea.value = text;
            textarea.setAttribute("readonly", "");
            textarea.style.position = "absolute";
            textarea.style.left = "-9999px";
            document.body.appendChild(textarea);
            textarea.select();
            const success = document.execCommand("copy");
            textarea.remove();
            return success;
        }
        catch (error) {
            console.error("Clipboard fallback copy failed.", error);
            return false;
        }
    };
};


/***/ },

/***/ "./src/DirectiveAthena.Website/wwwroot/ts/interop/download.ts"
/*!********************************************************************!*\
  !*** ./src/DirectiveAthena.Website/wwwroot/ts/interop/download.ts ***!
  \********************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   registerDownload: () => (/* binding */ registerDownload)
/* harmony export */ });
const registerDownload = () => {
    window.downloadFile = (fileName, content) => {
        const blob = new Blob([content], { type: "text/plain" });
        const url = window.URL.createObjectURL(blob);
        const anchor = document.createElement("a");
        anchor.href = url;
        anchor.download = fileName;
        anchor.click();
        window.URL.revokeObjectURL(url);
    };
};


/***/ },

/***/ "./src/DirectiveAthena.Website/wwwroot/ts/interop/fsApi.ts"
/*!*****************************************************************!*\
  !*** ./src/DirectiveAthena.Website/wwwroot/ts/interop/fsApi.ts ***!
  \*****************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   registerFsApi: () => (/* binding */ registerFsApi)
/* harmony export */ });
const DATABASE_NAME = "DirectiveAthenaDB";
const STORE_NAME = "handles";
const openIndexedDb = () => new Promise((resolve, reject) => {
    const request = indexedDB.open(DATABASE_NAME, 1);
    request.onupgradeneeded = () => {
        const db = request.result;
        if (!db.objectStoreNames.contains(STORE_NAME)) {
            db.createObjectStore(STORE_NAME);
        }
    };
    request.onsuccess = () => resolve(request.result);
    request.onerror = () => reject(request.error);
});
const storeHandle = async (key, handle) => {
    const db = await openIndexedDb();
    return new Promise((resolve, reject) => {
        const tx = db.transaction(STORE_NAME, "readwrite");
        tx.objectStore(STORE_NAME).put(handle, key);
        tx.oncomplete = () => resolve();
        tx.onerror = () => reject(tx.error);
        tx.onabort = () => reject(tx.error);
    });
};
const getHandle = async (key) => {
    const db = await openIndexedDb();
    return new Promise((resolve) => {
        const tx = db.transaction(STORE_NAME, "readonly");
        const request = tx.objectStore(STORE_NAME).get(key);
        request.onsuccess = () => resolve(request.result ?? null);
        request.onerror = () => resolve(null);
    });
};
const clearHandles = async () => {
    const db = await openIndexedDb();
    return new Promise((resolve, reject) => {
        const tx = db.transaction(STORE_NAME, "readwrite");
        tx.objectStore(STORE_NAME).clear();
        tx.oncomplete = () => resolve();
        tx.onerror = () => reject(tx.error);
        tx.onabort = () => reject(tx.error);
    });
};
const ensureRootHandle = async () => {
    const rootHandle = await getHandle("repo-root");
    if (!rootHandle) {
        throw new Error("No root handle found");
    }
    return rootHandle;
};
const getPathParts = (relativePath) => relativePath.split(/[\\/]/).filter((part) => part.length > 0);
const registerFsApi = () => {
    const fsApi = {
        isSupported: () => "showDirectoryPicker" in window,
        requestAccess: async () => {
            try {
                if (!window.showDirectoryPicker) {
                    return false;
                }
                const handle = await window.showDirectoryPicker({
                    mode: "readwrite"
                });
                await storeHandle("repo-root", handle);
                return true;
            }
            catch (error) {
                console.error("Access denied or failed", error);
                return false;
            }
        },
        hasAccess: async () => {
            const handle = await getHandle("repo-root");
            if (!handle) {
                return false;
            }
            const permission = await handle.queryPermission?.({ mode: "readwrite" });
            if (!permission) {
                return false;
            }
            if (permission === "granted") {
                return true;
            }
            if (permission === "prompt") {
                // We don't want to prompt automatically, just return false so UI can show "Grant" button.
                return false;
            }
            return false;
        },
        verifyPermission: async () => {
            const handle = await getHandle("repo-root");
            if (!handle) {
                return false;
            }
            const permission = await handle.requestPermission?.({ mode: "readwrite" });
            return permission === "granted";
        },
        resetAccess: async () => {
            await clearHandles();
            return true;
        },
        writeFile: async (relativePath, content) => {
            try {
                const rootHandle = await ensureRootHandle();
                const parts = getPathParts(relativePath);
                if (parts.length === 0) {
                    throw new Error("Invalid path: " + relativePath);
                }
                let currentHandle = rootHandle;
                for (let i = 0; i < parts.length - 1; i++) {
                    currentHandle = await currentHandle.getDirectoryHandle(parts[i], {
                        create: true
                    });
                }
                const fileHandle = await currentHandle.getFileHandle(parts[parts.length - 1], { create: true });
                const writable = await fileHandle.createWritable();
                await writable.write(content);
                await writable.close();
                return true;
            }
            catch (error) {
                console.error("Failed to write file: " + relativePath, error);
                return false;
            }
        },
        readFile: async (relativePath) => {
            try {
                const rootHandle = await ensureRootHandle();
                const parts = getPathParts(relativePath);
                if (parts.length === 0) {
                    throw new Error("Invalid path: " + relativePath);
                }
                let currentHandle = rootHandle;
                for (let i = 0; i < parts.length - 1; i++) {
                    currentHandle = await currentHandle.getDirectoryHandle(parts[i]);
                }
                const fileHandle = await currentHandle.getFileHandle(parts[parts.length - 1]);
                const file = await fileHandle.getFile();
                return await file.text();
            }
            catch (error) {
                console.error("Failed to read file: " + relativePath, error);
                return null;
            }
        },
        deleteFile: async (relativePath) => {
            try {
                const rootHandle = await ensureRootHandle();
                const parts = getPathParts(relativePath);
                if (parts.length === 0) {
                    throw new Error("Invalid path: " + relativePath);
                }
                let currentHandle = rootHandle;
                for (let i = 0; i < parts.length - 1; i++) {
                    currentHandle = await currentHandle.getDirectoryHandle(parts[i]);
                }
                await currentHandle.removeEntry(parts[parts.length - 1]);
                return true;
            }
            catch (error) {
                if (error instanceof DOMException && error.name === "NotFoundError") {
                    return true;
                }
                console.error("Failed to delete file: " + relativePath, error);
                return false;
            }
        }
    };
    window.fsApi = fsApi;
};


/***/ },

/***/ "./src/DirectiveAthena.Website/wwwroot/ts/interop/loadingObserver.ts"
/*!***************************************************************************!*\
  !*** ./src/DirectiveAthena.Website/wwwroot/ts/interop/loadingObserver.ts ***!
  \***************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initLoadingObserver: () => (/* binding */ initLoadingObserver)
/* harmony export */ });
const initLoadingObserver = () => {
    const observer = new MutationObserver(() => {
        const app = document.getElementById("app");
        const loadingScreen = document.getElementById("loading-screen");
        if (!app) {
            return;
        }
        // If app contains content other than the loading screen, it means Blazor has rendered.
        const hasRendered = app.children.length > 1 ||
            (app.children.length === 1 && app.children[0]?.id !== "loading-screen");
        if (!hasRendered) {
            return;
        }
        if (loadingScreen && !loadingScreen.classList.contains("activated")) {
            loadingScreen.classList.add("activated");
            // Remove from DOM after animation completes.
            window.setTimeout(() => {
                loadingScreen.remove();
            }, 1000);
        }
        observer.disconnect();
    });
    const app = document.getElementById("app");
    if (app) {
        observer.observe(app, { attributes: true, childList: true, subtree: true });
    }
};


/***/ },

/***/ "./src/DirectiveAthena.Website/wwwroot/ts/interop/scroll.ts"
/*!******************************************************************!*\
  !*** ./src/DirectiveAthena.Website/wwwroot/ts/interop/scroll.ts ***!
  \******************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   registerScrollFunctions: () => (/* binding */ registerScrollFunctions)
/* harmony export */ });
const registerScrollFunctions = () => {
    window.scrollToElement = (elementId) => {
        const element = document.getElementById(elementId);
        if (element) {
            element.scrollIntoView({ behavior: "smooth" });
        }
    };
    window.registerScrollListener = (dotNetHelper) => {
        window.addEventListener("scroll", () => {
            dotNetHelper.invokeMethodAsync("OnScroll", window.scrollY);
        });
    };
};


/***/ }

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Check if module exists (development only)
/******/ 		if (__webpack_modules__[moduleId] === undefined) {
/******/ 			var e = new Error("Cannot find module '" + moduleId + "'");
/******/ 			e.code = 'MODULE_NOT_FOUND';
/******/ 			throw e;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other modules in the chunk.
(() => {
/*!***********************************************************!*\
  !*** ./src/DirectiveAthena.Website/wwwroot/ts/interop.ts ***!
  \***********************************************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _interop_clipboard__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./interop/clipboard */ "./src/DirectiveAthena.Website/wwwroot/ts/interop/clipboard.ts");
/* harmony import */ var _interop_download__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./interop/download */ "./src/DirectiveAthena.Website/wwwroot/ts/interop/download.ts");
/* harmony import */ var _interop_fsApi__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./interop/fsApi */ "./src/DirectiveAthena.Website/wwwroot/ts/interop/fsApi.ts");
/* harmony import */ var _interop_loadingObserver__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./interop/loadingObserver */ "./src/DirectiveAthena.Website/wwwroot/ts/interop/loadingObserver.ts");
/* harmony import */ var _interop_scroll__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./interop/scroll */ "./src/DirectiveAthena.Website/wwwroot/ts/interop/scroll.ts");





(0,_interop_loadingObserver__WEBPACK_IMPORTED_MODULE_3__.initLoadingObserver)();
(0,_interop_scroll__WEBPACK_IMPORTED_MODULE_4__.registerScrollFunctions)();
(0,_interop_clipboard__WEBPACK_IMPORTED_MODULE_0__.registerClipboard)();
(0,_interop_download__WEBPACK_IMPORTED_MODULE_1__.registerDownload)();
(0,_interop_fsApi__WEBPACK_IMPORTED_MODULE_2__.registerFsApi)();

})();

/******/ })()
;
//# sourceMappingURL=interop.bundle.min.js.map